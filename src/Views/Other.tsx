import React from "react";
import styled from "styled-components";
import Button from "../General/Button";
import { ViewPropType } from "../types";

const Other = (props: ViewPropType) => {
  return <div>Other</div>;
};

export default Other;
